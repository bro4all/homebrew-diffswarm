# homebrew-diffswarm

Homebrew tap for DiffSwarm.

## Install

```bash
brew tap bro4all/diffswarm
brew install diffswarm
```

Formula updates are published automatically from the DiffSwarm release workflow.
